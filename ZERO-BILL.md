# KING AI Public Companion Zero-Bill Policy

This public repository is documentation/showcase only. It must never contain production secrets, private migrations, customer data, identity/billing authority, runtime Cloudflare/Supabase configuration, R2/Queues, or paid AI API credentials.

Automatic quality validation runs on the owned VPS self-hosted runner. Public documentation generation may use OpenClaw/Codex on the VPS, but only reviewed public-safe output may be committed.

OpenClaw/Codex instruction:

```text
Treat KINGAIASE-public as a public companion only. Run validation on the owned VPS. Never copy private repository source, secrets, customer data, production database migrations, runtime credentials or authority configuration into this repo. Never add paid cloud resources or paid AI APIs. Commit only explicitly public documentation/assets.
```
